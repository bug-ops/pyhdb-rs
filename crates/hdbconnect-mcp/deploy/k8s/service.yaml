apiVersion: v1
kind: Service
metadata:
  name: hdbconnect-mcp
  labels:
    app: hdbconnect-mcp
spec:
  selector:
    app: hdbconnect-mcp
  ports:
  - port: 80
    targetPort: 8080
    name: http
  type: ClusterIP
